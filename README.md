# inventor_leaf_metal_details_export
Модуль iLogic (VBA Inventor) для пакетного экспорта деталей (листовой металл) из текущей сборки. Формирует отдельные DXF-файлы с указанием масштаба и складывает их в папку, создаваемую под сборку внутри выделенной директории.
